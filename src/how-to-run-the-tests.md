# How to run the tests

I am a trong beliver (with evidence) of writing tests.
So each example comes with a bunch of test.

You can run the tests in each folder by typing the command:

```
$ cargo test
```
